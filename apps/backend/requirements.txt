# Lambda container runtime installs via `pip install -r requirements.txt` (see Dockerfile).
# Keep aligned with [project] dependencies in pyproject.toml.

aioboto3>=13.0.0
beautifulsoup4>=4.12.0
boto3>=1.34.0
fastapi>=0.115.0
httpx>=0.27.0
mangum>=0.18.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
pypdf>=5.0.0
qdrant-client>=1.12.0
uvicorn[standard]>=0.32.0
