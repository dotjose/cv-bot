# Copy to `.env.local` for local Next dev (file is gitignored).
# CI sets NEXT_PUBLIC_API_URL from `terraform output -raw http_api_endpoint` at build time — not from S3 bucket secrets.

NEXT_PUBLIC_API_URL=http://localhost:8787
