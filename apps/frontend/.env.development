# Loaded only for `next dev`. Production does not use this file — set NEXT_PUBLIC_API_URL
# in GitHub Actions (secret) at `next build` time; see .github/workflows/deploy.yml.
NEXT_PUBLIC_API_URL=http://localhost:8787
