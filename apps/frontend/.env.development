# Loaded only for `next dev`. Production: NEXT_PUBLIC_API_URL is set in CI from
# `terraform output -raw http_api_endpoint` (see .github/workflows/deploy.yml).
NEXT_PUBLIC_API_URL=http://localhost:8787
